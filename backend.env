# Environment
NODE_ENV=development

# Server
PORT=3000
API_PREFIX=/api/v1
API_HOST=0.0.0.0

# Database - Local backend (Postgres via Docker)
DATABASE_URL="postgresql://gelatini:gelatini123@localhost:5433/gelatini_db?schema=public"

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=8h
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production
JWT_REFRESH_EXPIRES_IN=30d

# Redis (Cache) - Local backend (Redis via Docker)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Logging
LOG_LEVEL=debug
LOG_FORMAT=json

# CORS
CORS_ORIGIN=http://localhost:5173
CORS_CREDENTIALS=true

# Features
FEATURE_LOYALTY=true
FEATURE_COUPONS=true
FEATURE_REPORTS=true
FEATURE_CASHBACK=true

# Logging
LOG_LEVEL=debug

# Rate Limiting
RATE_LIMIT_ENABLED=false
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS
CORS_ORIGIN=http://localhost:5173

# Scale Integration (Toledo Prix 3 Fit)
SCALE_ENABLED=true
SCALE_MANUFACTURER=TOLEDO
SCALE_MODEL="PRIX 3 FIT"
SCALE_PROTOCOL=toledo_prix
# Windows: ajuste para a porta COM correta (ex: COM3)
SCALE_PORT=COM3
SCALE_BAUDRATE=9600
SCALE_DATABITS=8
SCALE_STOPBITS=1
SCALE_PARITY=none
SCALE_STABLE_ONLY=true
SCALE_READ_TIMEOUT_MS=1500
SCALE_PROXY_URL=
SCALE_ALLOW_MOCK_ON_ERROR=false
